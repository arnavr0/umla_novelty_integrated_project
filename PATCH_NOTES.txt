This source tree is an integrated defensive overlay.
If you are merging into the original Win32 GUI project, wire ScoreCommand(...) into the Convert button.
Use libumlec.cpp as the shared engine for normalization and scoring.
